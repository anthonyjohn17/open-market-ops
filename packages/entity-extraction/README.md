# @omo/entity-extraction (Phase 2+ — stub)

MVP embeds asset resolution in `@omo/signal-engine`. This package will split when wedges need rich entities (SaaS products, domains, liquidation assets).

See [docs/FUTURE-EXPANSION.md](../../docs/FUTURE-EXPANSION.md#3-entity-extraction-package-omoentity-extraction).
