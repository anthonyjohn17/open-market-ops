# Example CLI output (sanitized)

Generated with:

```bash
MOCK_FEEDS=true pnpm omo run workflows/hiring-signals.yaml --output examples/run-output --min-score 50
```

Committed so portfolio reviewers can inspect output without running the CLI.
